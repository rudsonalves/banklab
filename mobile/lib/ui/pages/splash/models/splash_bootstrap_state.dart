import '/data/services/cache/last_login/models/last_login_identity.dart';

class SplashBootstrapState {
  const SplashBootstrapState({this.lastLoginIdentity});

  final LastLoginIdentity? lastLoginIdentity;
}
