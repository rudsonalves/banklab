// import 'package:flutter/material.dart';

// import '../routes.dart';

// extension GoRouterPopUntilExtension on BuildContext {
//   /// Pops routes until it reaches a route with the given [routeTo].
//   /// If the route is not found, it pops everything.
//   void popUntilNamed(Routes routeTo) {
//     Navigator.of(this).popUntil(
//       (route) => route.settings.name == routeTo.name || route.isFirst,
//     );
//   }
// }
